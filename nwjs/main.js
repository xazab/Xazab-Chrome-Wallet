(async function run() {
    // console.log("blub")
    // await new Promise(r => setTimeout(r, 3000));  // sleep x ms
    // console.dir(chrome.extension.getBackgroundPage())
    // console.log(chrome.extension.getURL("popup.html"))
    // chrome.tabs.getCurrent(function(tab){ console.log(tab.id)})
    // chrome.tabs.getCurrent(function (tab) { console.log(tab.id) })
    // chrome.tabs.query({}, function (tab) { console.log(tab[0].id) })
})()